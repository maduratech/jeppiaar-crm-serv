# Backend (jeppiaar-crm-serv) – no secrets
# See CONNECT_SUPABASE.md in repo root for connecting a new Supabase DB.

# Supabase (same project as frontend). Use service_role key – never expose in frontend.
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Server
PORT=3001
APP_MODE=academy
# Optional: restrict routes when APP_MODE=academy (see index.js allowlist)

# CORS (comma-separated origins; leave empty to use built-in fallback)
# CORS_ORIGINS=http://localhost:5173,https://your-frontend.vercel.app

# WhatsApp (for webhook + optional outbound)
WHATSAPP_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
VERIFY_TOKEN=
# Optional: second branch
# WHATSAPP_TOKEN_AU=
# WHATSAPP_PHONE_NUMBER_ID_AU=

# Lead API base (used by bot to call /api/lead/whatsapp)
# Development: http://localhost:3001  |  Production: https://api.jeppiaaracademy.com
# API_BASE_URL=https://api.jeppiaaracademy.com

# Optional: SMTP (welcome emails, etc.)
# SMTP_HOST=
# SMTP_PORT=465
# SMTP_USER=
# SMTP_PASS=

# Optional: Razorpay (invoicing/payments)
# RAZORPAY_KEY_ID=
# RAZORPAY_KEY_SECRET=
# RAZORPAY_WEBHOOK_SECRET=
